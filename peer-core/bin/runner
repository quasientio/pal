#!/bin/bash

JVM_OPTS="-Xmx1024M -Xms512M -server"

SYSTEM_PROPS=-Dlogback.configurationFile=file://$PEER_HOME/conf/logback-runner.xml

java $JVM_OPTS $SYSTEM_PROPS com.ittera.cometa.util.AppRunner "$@"
