#!/bin/bash

#Kafka doesn't like something in the classpath we export in setenv.sh
unset CLASSPATH

for log in `$KAFKA_HOME/bin/kafka-topics.sh --bootstrap-server localhost:9092 --list`; do
	if [[ "$log" != "__consumer_offsets" ]]; then
		echo $log 
	fi
done
