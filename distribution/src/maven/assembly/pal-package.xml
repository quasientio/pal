<!--

    Copyright (C) 2025 Quasient Inc. <https://www.quasient.com>

    Use of this software is governed by the Business Source License 1.1
    included in the file LICENSE and at https://mariadb.com/bsl11

    Change Date: 2029-10-01
    Change License: Apache 2.0

-->
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.0 http://maven.apache.org/xsd/assembly-2.1.0.xsd">

  <id>bin</id>

  <formats>
    <format>zip</format>
    <format>tar.gz</format>
  </formats>

  <includeBaseDirectory>true</includeBaseDirectory>
  <baseDirectory>pal-${project.version}</baseDirectory>

  <!-- ───────────────────────── Files copied one-by-one ─────────────────────── -->

  <files>
    <file>
      <source>../modules/pal-cli/target/pal-${project.version}.jar</source>
      <outputDirectory>lib</outputDirectory>
    </file>
  </files>

  <!-- ───────────────────────── Bulk file sets ──────────────────────────────── -->

  <fileSets>

    <!-- top-level metadata, config, helper scripts -->
    <fileSet>
      <directory>..</directory>
      <includes>
        <include>README.md</include>
        <include>AUTHORS</include>
        <include>LICENSE</include>
        <include>THIRD_PARTY.md</include>
        <include>licenses/</include>

        <!-- sample config files -->
        <include>config/</include>

        <!-- export env variables for running on host -->
        <include>export_env.sh</include>
      </includes>

      <!-- exclude build-time files inside licenses/ -->
      <excludes>
        <!-- exclude config files ending in .xml (not .xml.example) -->
        <exclude>config/**/*.xml</exclude>

        <!-- License stuff to exclude -->
        <exclude>licenses/BSL_HEADER.txt</exclude>
        <exclude>licenses/THIRD_PARTY.ftl</exclude>
        <exclude>licenses/url-fixes.xml</exclude>
      </excludes>
    </fileSet>

    <!-- everything from ../bin -->
    <fileSet>
      <directory>../bin</directory>
      <outputDirectory>bin</outputDirectory>
      <includes>
        <include>pal</include>
        <include>start_etcd.sh</include>
        <include>setup_kafka.sh</include>
        <include>start_kafka.sh</include>
      </includes>
    </fileSet>

  </fileSets>
</assembly>
