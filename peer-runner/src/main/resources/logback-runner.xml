<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false" scan="true" scanPeriod="30 seconds">

	<property name="logs.dir" value="/home/libre/code/cometa/aj-impl/peer-core/logs"/>

	<!-- APPENDERS -->
	<appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%date %level [%thread] %logger{10} %message%n</pattern>
		</encoder>
	</appender>

	<appender name="runner" class="ch.qos.logback.core.FileAppender">
		<file>${logs.dir}/runner.log</file>
		<encoder>
			<pattern>%date %level [%thread] %logger{10} %message%n</pattern>
		</encoder>
	</appender>

	<appender name="kafka" class="ch.qos.logback.core.FileAppender">
		<file>${logs.dir}/kafka.log</file>
		<encoder>
			<pattern>%date %level [%thread] %logger{10} %message%n</pattern>
		</encoder>
	</appender>

	<appender name="zookeeper" class="ch.qos.logback.core.FileAppender">
		<file>${logs.dir}/zookeeper.log</file>
		<encoder>
			<pattern>%date %level [%thread] %logger{10} %message%n</pattern>
		</encoder>
	</appender>

	<!-- LOGGERS -->

	<logger name="com.ittera.cometa" level="debug" additivity="false">
		<appender-ref ref="runner"/>
	</logger>

	<logger name="org.apache.kafka" level="warn" additivity="false">
		<appender-ref ref="kafka"/>
	</logger>

	<logger name="org.apache.zookeeper" level="warn" additivity="false">
		<appender-ref ref="zookeeper"/>
	</logger>

	<root level="warn">
		<appender-ref ref="stdout"/>
	</root>

</configuration>
